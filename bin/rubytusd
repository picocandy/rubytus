#!/usr/bin/env ruby

require 'bundler'
Bundler.require(:default)

require 'goliath/runner'

api = Rubytus::API.new
runner = Goliath::Runner.new(ARGV, api)
runner.app = Goliath::Rack::Builder.build(Rubytus::API, api)
runner.run

